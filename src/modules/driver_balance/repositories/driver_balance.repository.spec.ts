import { DriverBalanceRepository } from './driver_balance.repository';

describe('DriverBalanceRepository', () => {
  it('should be defined', () => {
    expect(new DriverBalanceRepository()).toBeDefined();
  });
});
