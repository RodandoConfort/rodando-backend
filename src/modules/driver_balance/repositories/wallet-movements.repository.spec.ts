import { WalletMovementsRepository } from './wallet-movements.repository';

describe('WalletMovementsRepository', () => {
  it('should be defined', () => {
    expect(new WalletMovementsRepository()).toBeDefined();
  });
});
