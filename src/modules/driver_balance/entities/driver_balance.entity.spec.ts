import { DriverBalanceEntity } from './driver_balance.entity';

describe('DriverBalanceEntity', () => {
  it('should be defined', () => {
    expect(new DriverBalanceEntity()).toBeDefined();
  });
});
