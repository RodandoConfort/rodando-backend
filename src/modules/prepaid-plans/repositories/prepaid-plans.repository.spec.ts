import { PrepaidPlansRepository } from './prepaid-plans.repository';

describe('PrepaidPlansRepository', () => {
  it('should be defined', () => {
    expect(new PrepaidPlansRepository()).toBeDefined();
  });
});
