import { CashColletionsPointsEntity } from './cash_colletions_points.entity';

describe('CashColletionsPointsEntity', () => {
  it('should be defined', () => {
    expect(new CashColletionsPointsEntity()).toBeDefined();
  });
});
