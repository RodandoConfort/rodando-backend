import { CashColletionPointRepository } from './cash_colletion_points.repository';

describe('CashColletionsPointsRepository', () => {
  it('should be defined', () => {
    expect(new CashColletionPointRepository()).toBeDefined();
  });
});
